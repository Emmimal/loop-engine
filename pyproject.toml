[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "loop_engineering"
version = "0.1.0"
description = "Deterministic goal-directed execution loop vs linear baseline"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
